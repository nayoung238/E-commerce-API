server:
  port: 0

spring:
  application:
    name: account-service
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/account_service
    username: root
    password: nayoung1234


eureka:
  instance:
    instanceId: ${spring.application.name}:${spring.aplication.instance_id:${random.value}}