server:
  port: 0

spring:
  application:
    name: item-service
  data:
    redis:
      port: 6379
  kafka:
    producer:
      acks: all
    consumer:
      group-id: itemServiceGroup
    streams:
      application-id: item_application

logging:
  level:
    com.ecommerce.itemservice: DEBUG